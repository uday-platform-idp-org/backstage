# Arquivo .envrc para direnv
# Configura variáveis de ambiente para desenvolvimento

export NAMESPACE=backstage
export RELEASE_NAME=backstage
export CHART_PATH=helm-chart

# Kubernetes Context
export KUBECONFIG=~/.kube/config

# Helm
export HELM_EXPERIMENTAL_OCI=1

# ArgoCD
export ARGOCD_SERVER=argocd.cloududay.com.br
export ARGOCD_USERNAME=admin

# GitHub
export GITHUB_OWNER=your-username
export GITHUB_REPO=backstage-k8s

# Development
export BACKSTAGE_DEV_MODE=true
export LOG_LEVEL=debug

# Colors for terminal output
export TERM=xterm-256color

echo "🚀 Environment loaded for Backstage development"
echo "📦 Namespace: $NAMESPACE"
echo "🎛️  Release: $RELEASE_NAME"
echo "📊 Chart Path: $CHART_PATH"